package model.structure;

/**
 * Kelas berisi SuperCookie yang memberikan player status Super ketika dimakan.
 */
public class SuperCookie extends Cookie {
  private final int value = 100;

  public SuperCookie(String sprite) {
    super(sprite);
  }
}
